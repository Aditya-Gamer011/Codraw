1) Creating a website from scratch: The person says he wants to build some website, he should be able to build that. Codraw creates a repo in their github (we'll have users sign in using github Oauth), the app creates a repo for them, pushes code, nd enables github pages to publish it. I don't think just a browser extension will be enough. Maybe we'll require a combo of browser extension nd app, or maybe just the app will do. idk. u hear me out nd tell me. 2) The app should have a canvas displaying what the website is currently looking like. We start off with a blank white canvas, and then the user takes the lead. Say, the user instead of prompting, just circled/doodled some stuff on website x and said it wants his website to have certain elements from website x, we should be able to bring over those elements. Maybe I'm trying to make this a bit like unity-- u see it files, live preview, etc. all in 1 page. 3)Even after the first prompt/first version is ready, the user should be able to point to certain things, like make doodles to bring certain elements to certain places, create new elements in certain places, make arrows to shift positions, etc. Maybe even drag over images, place them on the page, draw arrows and a placeholder shape as to how he wants the image to appear on the page, nd boom thats how it should be done 4) After the user is satisfied, he should be able to click on a create pr button which creates a pr to his initial website with the changes.